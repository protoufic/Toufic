# SEO Changes Applied

- Removed all public `noindex`, `nofollow`, and `noarchive` directives.
- Removed the global `X-Robots-Tag: noindex` Vercel header.
- Changed `robots.txt` from blocking the whole website to allowing crawling.
- Added canonical URLs for every main page.
- Added unique titles and meta descriptions for seven canonical pages.
- Added unique Open Graph and Twitter/X metadata.
- Generated seven custom 1200 × 630 social preview images.
- Added Person, WebSite, WebPage, AboutPage, CollectionPage, ProfilePage, Article, and BreadcrumbList JSON-LD where accurate.
- Added static route HTML files at build time so crawlers receive page-specific metadata immediately.
- Added XML sitemap with canonical URLs and `lastmod` dates.
- Added image sitemap with contextual image captions.
- Added RSS discovery feed.
- Added `llms.txt` for machine-readable entity and page discovery.
- Added `press-kit.json` with factual media information.
- Added IndexNow key and submission command.
- Added Google, Bing, and Yandex verification-token support through environment variables.
- Added 48 px favicon, Apple touch icon, and 192/512 px web app icons.
- Added permanent redirects from old URLs to canonical pages.
- Added dynamic route metadata updates for client-side navigation.
- Replaced “private-preview campaign” footer wording with “independent campaign.”
- Preserved the website design, interactions, race data, and video system.
